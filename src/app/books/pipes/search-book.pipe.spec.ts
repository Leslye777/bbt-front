import { SearchBookPipe } from './search-book.pipe';

describe('SearchBookPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchBookPipe();
    expect(pipe).toBeTruthy();
  });
});
