import { SearchLoanPipe } from './search-loan.pipe';

describe('SearchLoanPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchLoanPipe();
    expect(pipe).toBeTruthy();
  });
});
